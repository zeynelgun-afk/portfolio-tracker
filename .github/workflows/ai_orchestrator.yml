name: AI Orchestrator
on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Mode'
        required: false
        default: 'daily'
        type: choice
        options:
          - daily
          - weekly
      dry_run:
        description: 'Dry run'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  contents: write

concurrency:
  group: finzora-orchestrator-${{ github.ref }}
  cancel-in-progress: false

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run orchestrator
        env:
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_GROUP_CHAT_ID: ${{ secrets.TELEGRAM_GROUP_CHAT_ID }}
          TELEGRAM_DM_CHAT_ID: ${{ secrets.TELEGRAM_DM_CHAT_ID }}
        run: |
          MODE="${{ github.event.inputs.mode || 'daily' }}"
          DRY="${{ github.event.inputs.dry_run || 'false' }}"
          if [ "$DRY" = "true" ]; then
            python scripts/ai_orchestrator.py --mode "$MODE" --dry-run
          else
            python scripts/ai_orchestrator.py --mode "$MODE" --dm
          fi

      - name: Commit watchlist score updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/watchlist.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "[orchestrator] ${{ github.event.inputs.mode || 'daily' }} scores"
            for i in 1 2 3; do
              git pull --rebase origin main && git push origin main && break
              echo "Push retry $i..."
              sleep 2
            done
          fi
