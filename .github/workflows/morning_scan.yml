name: Sabah Evren Taraması

on:
  schedule:
    - cron: '15 6 * * 1-5'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Tarama modu (balanced/dividend/aggressive/all)'
        required: false
        default: 'all'

permissions:
  contents: write

jobs:
  morning-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: Python setup
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Bagimliliklar
        run: pip install requests

      - name: Git config
        run: |
          git config user.name "Finzora AI"
          git config user.email "zeynelgun@users.noreply.github.com"

      - name: Taramayi calistir
        run: |
          MODE="${{ github.event.inputs.mode || 'all' }}"
          echo "Mod: $MODE"
          python3 scripts/full_universe_screener.py --mode "$MODE" --workers 8 2>&1
        env:
          PYTHONUNBUFFERED: 1

      - name: Ozet olustur
        id: ozet
        run: |
          python3 -c "
import json, os
lines = []
for mode in ['balanced','dividend','aggressive']:
    fname = f'data/daily_scan_{mode}.json'
    if os.path.exists(fname):
        d = json.load(open(fname))
        lines.append(f\"{mode}:{d.get('ekle_adaylari','?')}ekle/{d.get('izle_adaylari','?')}izle\")
print(' | '.join(lines) if lines else 'tarama tamamlandi')
" > /tmp/ozet.txt 2>/dev/null || echo 'tarama tamamlandi' > /tmp/ozet.txt
          cat /tmp/ozet.txt
          echo "info=$(cat /tmp/ozet.txt)" >> "$GITHUB_OUTPUT"

      - name: Sonuclari commit et
        run: |
          FILES="data/daily_scan_balanced.json data/daily_scan_dividend.json data/daily_scan_aggressive.json data/daily_full_scan.json"
          CHANGED=$(git status --porcelain $FILES 2>/dev/null)
          if [[ -n "$CHANGED" ]]; then
            echo "Degisiklik var, commit ediliyor..."
            TARIH=$(date +'%Y-%m-%d %H:%M UTC')
            for attempt in 1 2 3; do
              git pull --rebase origin main || true
              git add $FILES
              git diff --staged --quiet && echo "Commit edilecek yok" && exit 0
              git commit -m "[AUTO-SCAN] ${TARIH} | ${{ steps.ozet.outputs.info }}"
              git push && echo "Push basarili" && exit 0
              echo "Attempt $attempt basarisiz, bekleniyor..."
              sleep $((attempt * 10))
            done
            echo "Push basarisiz oldu" && exit 1
          else
            echo "Degisiklik yok."
          fi
