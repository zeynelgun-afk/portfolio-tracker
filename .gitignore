# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/

# Build
dist/
build/

# Secrets / local env
.env
.env.local
*.pem
*.key

# ── Finzora state dosyaları (runtime, commit edilmez) ────────────────────────
agent/memory/portfolio_state.json
agent/memory/daily_brief.json

# Seans durumu — KALDIRILDI: 21 Nisan 2026 teshis. data/session_state.json
# git-tracked olmali, yoksa kapanis -> sabah handoff'u calismiyor, sabah
# raporu "flag yok" diyor. GitHub Actions runner her run'da fresh, yerel
# "gecici" dosya paradigmasi bu mimaride ise yaramiyor.
# data/session_state.json

# Cache dosyaları
data/vix_cache.json
data/telegram_offset.json

# Observability (runtime state)
# NOT: logs/events.jsonl git'te tutuluyor — observability kaynağı
data/finzora.db
data/finzora.db-journal
data/finzora.db-shm
data/finzora.db-wal

# RAG vector store (JSONL'den rebuild edilebilir)
data/rag/chroma/
# v7: catalyst layer cache (FMP'den rebuild edilebilir, 6 saatlik TTL)
data/rag/catalyst_cache/

# Olay log
logs/event_log.jsonl
logs/*.log
!logs/.gitkeep
!logs/events.jsonl
!logs/archive/

# Sistem
.DS_Store
Thumbs.db
*.swp
*~

# IDE
.vscode/
.idea/
*.sublime-*

# Obsidian — kullanıcının kişisel vault config'i, repo'da olmasın
.obsidian/
**/.obsidian/
data/swing/active.json.bak-*
data/k13_pending_change.json

agent/data/
